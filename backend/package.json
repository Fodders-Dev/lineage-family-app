{
  "name": "lineage-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal self-hostable custom API backend for Lineage dev bootstrap",
  "main": "src/server.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "ws": "^8.18.3"
  }
}
